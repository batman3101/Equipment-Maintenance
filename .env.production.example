# ================================================================
# CNC 설비 관리 시스템 - 프로덕션 환경 변수 템플릿
# Equipment Maintenance System - Production Environment Variables Template
# ================================================================

# ⚠️ 주의: 이것은 예제 파일입니다. 실제 값은 .env.production 파일에 설정하세요.
# WARNING: This is an example file. Set actual values in .env.production file.

# Supabase 설정 (Required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_production_anon_key_here

# 관리자 전용 Service Role Key (서버에서만 사용)
# 🚨 보안: 절대로 클라이언트에 노출되지 않도록 주의!
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# 프로덕션 환경 설정
NODE_ENV=production

# 애플리케이션 설정
NEXT_PUBLIC_APP_NAME="CNC 설비 관리 시스템"
NEXT_PUBLIC_APP_VERSION="1.0.0"
NEXT_PUBLIC_COMPANY_NAME="Your Company Name"

# 베트남 지역화 설정
NEXT_PUBLIC_TIMEZONE="Asia/Ho_Chi_Minh"
NEXT_PUBLIC_LOCALE="vi-VN"
NEXT_PUBLIC_CURRENCY="VND"

# 프로덕션 보안 설정
NEXT_PUBLIC_DEBUG_MODE=false
NEXT_PUBLIC_MOCK_DATA=false
NEXT_PUBLIC_OFFLINE_MODE=false

# 파일 업로드 설정
NEXT_PUBLIC_MAX_FILE_SIZE=10485760
NEXT_PUBLIC_ALLOWED_FILE_TYPES="image/jpeg,image/png,image/webp,application/pdf"
NEXT_PUBLIC_STORAGE_BUCKET=equipment-images

# 성능 설정
NEXT_PUBLIC_ITEMS_PER_PAGE=20
NEXT_PUBLIC_MAX_SEARCH_RESULTS=100
NEXT_PUBLIC_AUTO_REFRESH_INTERVAL=60000

# 보안 헤더 설정
NEXT_PUBLIC_CSP_ENABLED=true
NEXT_PUBLIC_HSTS_ENABLED=true

# ================================================================
# 설정 가이드:
# ================================================================
# 1. 이 파일을 복사하여 .env.production 파일을 생성하세요
# 2. 실제 API 키와 비밀 정보를 입력하세요
# 3. .env.production 파일은 절대 Git에 커밋하지 마세요
# 4. Vercel 배포시 Dashboard에서 환경 변수를 설정하세요
# 5. Service Role Key는 서버 사이드에서만 사용하세요
# ================================================================