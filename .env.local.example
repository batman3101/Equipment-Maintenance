# ================================================================
# CNC 설비 관리 시스템 - 환경 변수 설정 (베트남 현지화)
# Equipment Maintenance System - Environment Variables Configuration
# ================================================================

# Supabase Database 설정 (Required)
# 1. https://supabase.com 에서 새 프로젝트 생성
# 2. Settings > API 에서 아래 값들을 복사해서 입력
# 3. Database에 supabase-schema-optimized.sql 실행 필요
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key (Admin operations, 선택사항)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# 개발 환경 설정
NODE_ENV=development

# 애플리케이션 기본 설정
NEXT_PUBLIC_APP_NAME="CNC 설비 관리 시스템"
NEXT_PUBLIC_APP_VERSION="1.0.0"
NEXT_PUBLIC_COMPANY_NAME="회사명을 입력하세요"

# 베트남 지역화 설정
NEXT_PUBLIC_TIMEZONE="Asia/Ho_Chi_Minh"
NEXT_PUBLIC_LOCALE="vi-VN"
NEXT_PUBLIC_CURRENCY="VND"

# 시스템 설정
NEXT_PUBLIC_MAINTENANCE_ALERT_DAYS=7
NEXT_PUBLIC_AUTO_REFRESH_INTERVAL=30000
NEXT_PUBLIC_MAX_FILE_SIZE=10485760
NEXT_PUBLIC_ALLOWED_FILE_TYPES="image/jpeg,image/png,image/webp,application/pdf"

# 보안 설정 (Supabase Auth 사용으로 NextAuth 불필요)
# NEXTAUTH_SECRET=your_nextauth_secret_here  # 사용 안함
# NEXTAUTH_URL=http://localhost:3000          # 사용 안함

# 개발 모드 설정
NEXT_PUBLIC_DEBUG_MODE=false
NEXT_PUBLIC_MOCK_DATA=true

# 업로드 설정 (Supabase Storage)
NEXT_PUBLIC_STORAGE_BUCKET=equipment-images
NEXT_PUBLIC_MAX_IMAGE_SIZE=5242880

# 페이지네이션 설정
NEXT_PUBLIC_ITEMS_PER_PAGE=20
NEXT_PUBLIC_MAX_SEARCH_RESULTS=100

# ================================================================
# 사용법 (How to use):
# ================================================================
# 1. 이 파일을 .env.local 로 복사
#    Copy this file to .env.local
#
# 2. Supabase 프로젝트 생성 및 설정
#    - https://supabase.com 에서 새 프로젝트 생성
#    - Settings > API 에서 URL과 anon key 복사
#    - SQL Editor에서 supabase-schema-optimized.sql 실행
#
# 3. 실제 값으로 변경
#    Replace placeholder values with actual values
#
# 4. 개발 서버 실행
#    npm run dev
#
# 5. Vercel 배포시 환경변수 설정
#    Set environment variables in Vercel Dashboard → Settings → Environment Variables
#
# ================================================================
# 베트남 특화 설정 참고사항:
# - 시간대: Asia/Ho_Chi_Minh (GMT+7)
# - 통화: Vietnamese Dong (VND)
# - 비상전화: 113 (경찰), 114 (소방서), 115 (응급의료)
# - 근무시간: 08:00-17:00 (점심시간 12:00-13:00)
# ================================================================